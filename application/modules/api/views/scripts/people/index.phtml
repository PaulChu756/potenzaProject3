<br /><br />

<?php 
	foreach ($this->entries as $entry)
	{
		echo json_encode($this->escape($entry->firstName), JSON_PRETTY_PRINT);
		echo json_encode($this->escape($entry->lastName), JSON_PRETTY_PRINT);
		echo json_encode($this->escape($entry->favoriteFood), JSON_PRETTY_PRINT) . "<br>";
	}
	
?>